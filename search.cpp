#include "Route.h"
#include "RouteNode.h"

using namespace std;

vector<Routen>getRoutes(string airportCode, unordered_map<string, vector<Route>>& routes) {
    vector<Route> destAirports;
    for (auto Route : routes){
        if (Route.)
    }
}
